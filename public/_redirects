# Retired Edge Atlas -> honest outcome table (permanent)
/edge-atlas /outcomes 301
/edge-atlas/* /outcomes 301

# SPA route fallback for static hosting (Netlify/Cloudflare Pages compatible)
# Ensures deep links like /auth or /admin/login load index.html instead of a raw 404.
/* /index.html 200

